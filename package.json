{
  "name": "@nova-fe/color",
  "version": "1.0.0",
  "description": "原子化css，提供基础色，维护主题色css变量。",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "pnpm run clean && ts-node scripts/system.ts",
    "prepublishOnly": "pnpm run build",
    "check:build": "ls -la dist/system/",
    "check:publish": "pnpm pack --dry-run",
    "release:patch": "pnpm version patch && pnpm publish",
    "release:minor": "pnpm version minor && pnpm publish",
    "release:major": "pnpm version major && pnpm publish",
    "check:pre-publish": "node scripts/check-publish.js"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "author": "Leslie <924361501@qq.com>",
  "license": "ISC",
  "devDependencies": {
    "@ctrl/tinycolor": "^4.1.0",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.15.30",
    "fs-extra": "^11.3.0",
    "path": "^0.12.7",
    "rimraf": "^6.0.1",
    "sass": "^1.89.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
